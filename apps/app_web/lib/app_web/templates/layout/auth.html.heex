<main class="max-w-[min(90%,33em)] p-4 my-4 mx-auto">
  <h1 class="text-2xl uppercase"><%= @page_title %></h1>

  <%= for {type, message} <- get_flash(@conn) do %>
    <.alert type={type}><%= message %></.alert>
  <% end %>

  <%= @inner_content %>
</main>
